<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:SCUScanner.Helpers"
             x:Class="SCUScanner.Pages.InputSerialForSparesPage">
    <ContentPage.Content>
        <StackLayout Padding="5">
            <Entry x:Name="eSerialNumber" Placeholder="{Binding Resources[EnterSerialNumberText]}" Text="{Binding SerialNumber}" >
                <Entry.Effects>
                    <controls:ClearEntryEffect />
                </Entry.Effects>
            </Entry>
            <StackLayout Orientation="Horizontal" HorizontalOptions="Center"> 
                <Button x:Name="bScanQR" Text="{Binding Resources[ScanQRCode]}"   WidthRequest="150"
                    BackgroundColor="Green" TextColor="White"></Button>
                <Button x:Name="SparesStart" Text="{Binding Resources[SparesText]}" Clicked="SparesStart_Clicked" WidthRequest="150"
                    VerticalOptions="EndAndExpand" BackgroundColor="Green" TextColor="White"></Button>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>