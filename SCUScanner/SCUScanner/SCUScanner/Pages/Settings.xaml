<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
              xmlns:local="clr-namespace:SCUScanner.Resources"
             x:Class="SCUScanner.Pages.Settings">
    <ContentPage.Content>
        <StackLayout >
            <!--<Label Text="{local:Translate SettingsText}"/>-->
            <Grid Padding="10,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="*" />

                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    
                    <RowDefinition />
                </Grid.RowDefinitions>
                <!--Text="{local:Translate ContinuousScanText}}"-->
                <Label  Grid.Column="0" Grid.Row="0" Text="{Binding Resources[ContinuousScanText]}" VerticalTextAlignment="Center" />
                <Switch BindingContext="{Binding Settings}" Grid.Column="1" Grid.Row="0" IsToggled="{Binding ScanMode}"/>
                <Label Grid.Column="2" Grid.Row="0" Text="{Binding Resources[ManualScanText]}" VerticalTextAlignment="Center"/>


                <StackLayout Grid.Column="0" Grid.Row="1" Orientation="Horizontal" Grid.ColumnSpan="2" >
                    <Label  Text="{Binding Resources[SelectedLanguageCodeText]}"  VerticalTextAlignment="Center"/>
                    <Label  BindingContext="{Binding Settings}" Text="{Binding SelectedLang}" VerticalTextAlignment="Center" />
                </StackLayout>

                <Button Grid.Column="1" Grid.Row="2" Command="{Binding SelectLangCommnad}" Text=">" VerticalOptions="Center"/>
            </Grid>
            
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>