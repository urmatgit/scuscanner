<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mr="clr-namespace:MR.Gestures;assembly=MR.Gestures"
             xmlns:sfImage="clr-namespace:Syncfusion.SfImageEditor.XForms;assembly=Syncfusion.SfImageEditor.XForms"
             xmlns:views="clr-namespace:SCUScanner.Pages.Views"
              NavigationPage.HasNavigationBar="false"
   NavigationPage.HasBackButton="false"
             x:Class="SCUScanner.Pages.SparePage">
    <ContentPage.Content>
        <StackLayout>
            <views:NavigationBarView x:Name="NavigationBarView"  Title="{Binding CartCount}" />
        <StackLayout VerticalOptions="FillAndExpand">
            <mr:StackLayout VerticalOptions="Fill" HorizontalOptions="Fill" >
            <sfImage:SfImageEditor x:Name="imgViewer" PanningMode="SingleFinger"  Source="{Binding ImageSpare}"  >
            <sfImage:SfImageEditor.ToolbarSettings>
                <sfImage:ToolbarSettings IsVisible="false" />
            </sfImage:SfImageEditor.ToolbarSettings>
            
        </sfImage:SfImageEditor>
        
        </mr:StackLayout>
                    <Button   Text="AddTest random" Command="{Binding AddCommand}" BackgroundColor="Green" TextColor="White" VerticalOptions="EndAndExpand"></Button>
            
        </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>